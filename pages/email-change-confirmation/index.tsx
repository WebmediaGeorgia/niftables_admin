import React from 'react';
import { EmailChangeConfirmation } from '@components/EmailConfirmation';
import { MainLayoutStatic } from '@components/MainLayout';

const EmailChangeConfirmationPage = () => {
  return (
    <MainLayoutStatic>
      <EmailChangeConfirmation />
    </MainLayoutStatic>
  );
};

export default EmailChangeConfirmationPage;
